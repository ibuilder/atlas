"""Business services.

One package per bounded context. Services own transactions, enforce policy, and
emit audit events; route handlers translate HTTP into service calls and back.

SPDX-License-Identifier: MIT
"""
